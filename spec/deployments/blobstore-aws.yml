---
kit:
  name:    dev
  features:
    - partitioned-network
    - bare
    - aws-blobstore

genesis:
  env:         blobstore-aws

params:
  base_domain: cf.testing.examle
  blobstore_s3_region: blobstore-s3-region
