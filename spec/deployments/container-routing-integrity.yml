---
kit:
  name:    dev
  features:
    - partitioned-network
    - bare
    - container-routing-integrity

genesis:
  env:         container-routing-integrity

params:
  base_domain: cf.testing.examle

