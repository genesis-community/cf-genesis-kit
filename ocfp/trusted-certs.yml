---
- type: replace
  path: /instance_groups/name=diego-cell/jobs/name=cflinuxfs3-rootfs-setup/properties/cflinuxfs3-rootfs/trusted_certs/-
  value: (( join "" isolation_segments.{{segment-name}}.base_trusted_certs meta.ocfp.certs.trusted ))

- type: replace
  path: /instance_groups/name=diego-cell/jobs/name=rep/properties/containers/trusted_ca_certificates/-
  value: (( join "" isolation_segments.{{segment-name}}.base_trusted_certs meta.ocfp.certs.trusted ))

#instance_groups:
#- name: diego-cell
#  jobs:
#    - name: rep
#      properties:
#        containers:
#          trusted_ca_certificates:
#            - (( append ))
#            - (( grab meta.ocfp.certs.trusted ))
#    - name: cflinuxfs3-rootfs-setup
#      properties:
#        cflinuxfs3-rootfs:
#          trusted_certs:
#            - (( append ))
#            - (( grab meta.ocfp.certs.trusted ))
