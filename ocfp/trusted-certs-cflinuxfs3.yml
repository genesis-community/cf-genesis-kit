instance_groups:
- name: diego-cell
  jobs:
    - name: cflinuxfs3-rootfs-setup
      properties:
        cflinuxfs3-rootfs:
          trusted_certs:
            - (( append ))
            - (( join "" meta.ocfp.certs.trusted ))
