instance_groups:
- name: diego-cell
  jobs:
    - name: cflinuxfs4-rootfs-setup
      properties:
        cflinuxfs4-rootfs:
          trusted_certs:
            - (( append ))
            - (( join "" meta.ocfp.certs.trusted ))

