bosh-variables:
  app_package_directory_key: (( grab params.blobstore_app_packages_directory || meta.blobstore_bucket_path.app-packages ))
  buildpack_directory_key:   (( grab params.blobstore_buildpacks_directory   || meta.blobstore_bucket_path.buildpacks   ))
  droplet_directory_key:     (( grab params.blobstore_droplets_directory     || meta.blobstore_bucket_path.droplets     ))
  resource_directory_key:    (( grab params.blobstore_resources_directory    || meta.blobstore_bucket_path.resources    ))
